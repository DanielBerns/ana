[project]
name = "ana"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.uv.sources]
shared = { workspace = true }

[tool.uv.workspace]
members = [
    "packages/shared",
    "apps/edge/*",
    "apps/core/*"
]

